# Security model

Controls: Supabase Auth, relationship-aware RLS, server-side permission checks,
private storage, immutable signed versions, webhook HMAC/replay protection,
idempotency keys, transaction locks, event audit and data-minimizing portal
bundles. Sensitive documents are never public. Monetary mutation occurs in SQL.

